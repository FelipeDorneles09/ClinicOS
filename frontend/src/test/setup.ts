/**
 * Setup global para os testes do Vitest.
 * Importa os matchers do @testing-library/jest-dom para que
 * assertions como toBeInTheDocument(), toHaveTextContent(), etc.
 * estejam disponíveis em todos os arquivos de teste.
 */
import "@testing-library/jest-dom";
